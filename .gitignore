# deps
node_modules/

# Next.js build output
.next/
out/
dist/

# env files — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# system
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel

# VPS deploy scripts contain secrets — never commit
vps-deploy.sh
vps-deploy-portal.sh
vps-setup-content.sh

# TypeScript build info
*.tsbuildinfo

# Local project registry (dev fallback when /var/www/portal-content is unwritable)
.registry.json
.registry.json.tmp-*
